export interface Book {
  id?: number;
  title: string;
  author: string;
  read: boolean;
  //description?: string;
  //publishedDate?: string; // ISO date string
  //status?: 'to-read' | 'reading' | 'finished';
}
